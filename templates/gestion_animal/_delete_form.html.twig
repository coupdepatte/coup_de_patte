<form method="post" action="{{ path('app_gestion_animal_delete', {'idAnimal': animal.idAnimal}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal.idAnimal) }}">
    <button class="btn">Delete</button>
</form>
